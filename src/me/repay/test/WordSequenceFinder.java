package me.repay.test;

import java.util.Collection;

/**
 * Finds sequences of words in an input text.
 * 
 * The result should contain:
 * - Each contained word exactly once except stop words
 * - Sequences of this word followed by the next one or two successors
 * - Sequences should stop at stop words
 * 
 * Example:
 * 'This is a text' should return 'This', 'This is', 'is', 'text' in arbitrary order.
 * 
 * Test with some other sentences.
 */
public class WordSequenceFinder {


	private static final String[] STOP_WORDS = {"a", "and", "the", "you"};
	
	public static Collection<String> findWordSequencesIn(String input) {
		return null;
	}
	
	
}
